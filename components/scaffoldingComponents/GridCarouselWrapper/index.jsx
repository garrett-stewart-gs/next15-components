"use client";
import React from "react";

import { useActiveIndex } from "@/utils/hooks/useActiveIndex";
import { useHorizontalSwipeTracker, useVerticalSwipeTracker } from "@/utils/hooks/useSwipeTrackers";

export default function GridCarouselWrapper({ handleTouchStart, handleTouchMove, handleTouchEnd, transitionSpeed = 0.15, children }) {

  return (
    <>
    </>
  );
}
